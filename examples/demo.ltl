// examples/demo.ltl — runnable demo for ltl_xml_parser

import std::io
use ltl_xml_parser as lib

fn main() {
    io::println("ltl_xml_parser demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}