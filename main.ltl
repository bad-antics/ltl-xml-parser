// ltl-xml-parser/main.ltl
//
// Pull-style XML parser (SAX-like) with entity expansion.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-xml-parser v0.1.0")
    io::println("Pull-style XML parser (SAX-like) with entity expansion.")
    0
}
